{
  "name": "banco-virtual-backend",
  "version": "1.0.0",
  "description": "Backend simple para banco virtual usando SQLite + Prisma, SIN JWT ni MFA",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "prisma": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "axios": "^1.4.0"
  }
}
